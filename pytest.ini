[pytest]
testpaths = tests
addopts = -m "not skip" --ignore=tests/unit/test_stepmotor.py
markers =
    smoke: mark test as smoke test
    regression: mark test as regression test
    integration: mark test as integration test (requires live API connectivity)